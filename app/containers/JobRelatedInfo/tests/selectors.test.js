// import { selectJobRelatedInfoDomain } from '../selectors';

describe('selectJobRelatedInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
