/*
 * AdminCandidatesListActions Messages
 *
 * This contains all the text for the AdminCandidatesListActions container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminCandidatesListActions';

export default defineMessages({
  deleteCandidates: {
    id: `${scope}.header`,
    defaultMessage: 'Delete Selected Candidates',
  },
});
