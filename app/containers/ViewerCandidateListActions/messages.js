/*
 * ViewerCandidateListActions Messages
 *
 * This contains all the text for the ViewerCandidateListActions container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewerCandidateListActions';

export default defineMessages({
  requestInfo: {
    id: `${scope}.header`,
    defaultMessage: 'Request information',
  },
});
