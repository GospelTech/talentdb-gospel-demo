// import { selectViewerCandidateListActionsDomain } from '../selectors';

describe('selectViewerCandidateListActionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
