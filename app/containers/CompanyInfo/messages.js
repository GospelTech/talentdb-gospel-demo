/*
 * CompanyInfo Messages
 *
 * This contains all the text for the CompanyInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CompanyInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CompanyInfo container!',
  },
});
