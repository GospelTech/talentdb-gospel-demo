/*
 * CandidateApprovalTable Messages
 *
 * This contains all the text for the CandidateApprovalTable container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CandidateApprovalTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CandidateApprovalTable container!',
  },
});
