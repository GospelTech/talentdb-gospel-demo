// import { selectCandidateApprovalTableDomain } from '../selectors';

describe('selectCandidateApprovalTableDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
