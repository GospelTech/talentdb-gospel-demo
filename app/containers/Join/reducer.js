/*
 *
 * Join reducer
 *
 */
import produce from 'immer';

export const initialState = {};

// TODO: remove
/* eslint-disable default-case, no-param-reassign */
const joinReducer = (state = initialState) => produce(state, () => {});

export default joinReducer;
