// import { selectCandidateActionsDomain } from '../selectors';

describe('selectCandidateActionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
