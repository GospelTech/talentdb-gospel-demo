/*
 * ViewerCandidateList Messages
 *
 * This contains all the text for the ViewerCandidateList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ViewerCandidateList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Candidates List',
  },
});
