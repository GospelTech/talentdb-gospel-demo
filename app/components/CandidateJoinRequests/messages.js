/*
 * CandidateJoinRequests Messages
 *
 * This contains all the text for the CandidateJoinRequests component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CandidateJoinRequests';

export default defineMessages({
  candidateRequests: {
    id: `${scope}.header`,
    defaultMessage: `{new} Candidate Account Requests`,
  },
});
