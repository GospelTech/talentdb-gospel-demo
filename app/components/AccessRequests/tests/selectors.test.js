// import { selectAccessRequestsDomain } from '../selectors';

describe('selectAccessRequestsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
