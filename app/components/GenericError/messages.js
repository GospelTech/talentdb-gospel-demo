/*
 * GenericError Messages
 *
 * This contains all the text for the GenericError component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GenericError';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GenericError component!',
  },
});
