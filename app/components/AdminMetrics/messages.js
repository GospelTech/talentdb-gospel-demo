/*
 * AdminMetrics Messages
 *
 * This contains all the text for the AdminMetrics component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AdminMetrics';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminMetrics component!',
  },
});
