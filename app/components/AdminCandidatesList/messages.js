/*
 * AdminCandidatesList Messages
 *
 * This contains all the text for the AdminCandidatesList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AdminCandidatesList';

export default defineMessages({
  candidatesList: {
    id: `${scope}.header`,
    defaultMessage: 'Candidates List',
  },
});
