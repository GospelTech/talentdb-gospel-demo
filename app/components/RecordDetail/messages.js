/*
 * RecordDetail Messages
 *
 * This contains all the text for the RecordDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RecordDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecordDetail component!',
  },
});
